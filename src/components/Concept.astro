---
import { siteConfig } from '../data/siteConfig';
const { concept: conceptImg } = siteConfig.images;
const { concept: conceptText } = siteConfig.content;
---

<section id="concept" class="py-24 sm:py-32 bg-cream-50 text-noir-900 relative overflow-hidden">
  <!-- Decorative watermark -->
  <div class="absolute -right-16 top-1/2 -translate-y-1/2 pointer-events-none opacity-[0.03] select-none">
    <span class="font-serif text-[18rem] leading-none">Pastry</span>
  </div>

  <div class="max-w-[1560px] mx-auto px-6 sm:px-8 lg:px-12 scroll-fade-in">
    <!-- Section Header -->
    <div class="text-center max-w-3xl mx-auto space-y-3 mb-16 sm:mb-24">
      <span class="text-xs font-sans tracking-mega-wide uppercase text-gold-600 block">{conceptText.badge}</span>
      <h2 class="text-3xl sm:text-4xl lg:text-5xl font-serif-title tracking-ultra-wide font-light uppercase">
        {conceptText.titleEn}
      </h2>
      <div class="w-12 h-[1px] bg-gold-400 mx-auto"></div>
      <p class="font-serif-jp text-sm sm:text-base text-stone-600 tracking-widest pt-2">{conceptText.subtitleJa}</p>
    </div>

    <!-- Editorial Layout -->
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 lg:gap-20 items-center">
      <!-- Image Left -->
      <div class="lg:col-span-6 relative">
        <div class="relative z-10 overflow-hidden shadow-2xl">
          <img
            src={conceptImg.main}
            alt="Pastry Chef crafting cakes"
            class="w-full h-[480px] sm:h-[600px] lg:h-[660px] object-cover hover:scale-105 transition-transform duration-700"
          />
        </div>
        <!-- Floating secondary small image -->
        <div class="hidden sm:block absolute -bottom-8 -right-8 w-60 h-60 z-20 overflow-hidden shadow-xl">
          <img
            src={conceptImg.sub}
            alt="Handcrafted pastry details"
            class="w-full h-full object-cover"
          />
        </div>
      </div>

      <!-- Text Right -->
      <div class="lg:col-span-6 space-y-6 lg:pl-6">
        <div class="space-y-5">
          <h3 class="text-2xl sm:text-3xl font-serif-jp font-light leading-relaxed tracking-wide text-noir-900">
            {conceptText.lead}
          </h3>
          {conceptText.paragraphs.map(p => (
            <p class="text-stone-600 font-serif-jp text-sm sm:text-base leading-loose tracking-wide">
              {p}
            </p>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>
